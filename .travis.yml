language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jmkitavi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jmkitavi/docker-react yarn test
